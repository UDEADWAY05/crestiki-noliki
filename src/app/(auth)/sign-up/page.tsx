"use client";

import { SignUpForm } from "@/features/auth/ui/sign-up-form";

export default function LoginPage() {

    return (
        <div className="p-5 flex w-screen h-screen items-center justify-center">
            <SignUpForm />
        </div>
    );
}